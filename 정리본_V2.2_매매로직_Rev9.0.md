# 정리본_V2.2_매매로직_Rev9.0

무한매수법 V2.2 전략 명세서

Rev9.0 최종수정본 · 자동매매 구현용 체결 규칙 중심 문서

<!-- table_1 -->
| 문서 목적: 기존 Rev9.0 서술형 가이드를 자동매매 구현용 전략 명세서 형식으로 재구성한 문서입니다. 예시 설명보다 계산 규칙·상태 규칙·주문 생성 규칙·체결 후 갱신 규칙을 우선합니다. |
| --- |

<!-- table_2 -->
| 재구성 원칙• 본 문서는 기존 서술형 가이드를 자동매매 구현용 전략 명세서로 재구성한 버전이다.• 예시 시나리오를 본문에서 제거하고, 계산 규칙·상태 판정·주문 생성·체결 후 갱신 규칙 순으로 정리한다.• V2.2의 고유 성격인 고정 1회 매수시도액, 전반전/후반전 분리, 쿼터손절모드의 별도 생존 로직을 유지한다. |
| --- |

1. 전략 범위와 문서 사용 원칙

• 범위: 본 문서는 V2.2 전략의 주문 생성·체결 반영·상태 전이 규칙을 자동매매 구현 관점에서 정의한다.

• 우선순위: 예시 숫자보다 일반식과 상태 규칙을 우선하며, 문서의 모든 주문은 “어떤 조건에서 어떤 주문세트를 생성하는가”로 기술한다.

• 주문 구조 원칙: 일일 주문세트의 기본 구조는 “기본 주문세트 + 줍줍매수 사다리”다. 큰수매수는 별도 전략이 아니라, 브로커 가격 괴리 규칙으로 상단 매수부가 거부되거나 거부가 예상될 때 기본 주문세트의 상단 매수부만 재구성하는 규칙이다.

• 체결 기준: 주문 생성은 전일 확정 체결을 반영한 뒤 수행하며, 당일 중 발생한 체결 결과는 다음 주문 생성 시점의 상태값으로 반영한다.

2. 핵심 용어·기호·정의·수식

<!-- table_3 -->
| 항목 | 기호 | 정의 | 일반식 / 해석 |
| --- | --- | --- | --- |
| 총 원금 | P | 전략에 단독으로 할당한 시작 투자금. 사이클 시작 시 분할 기준의 모수로 사용. | P > 0 |
| 총 분할 수 | a | 전체 원금을 몇 회의 주문 단위로 나누어 운용할지 나타내는 총 분할 수. | a ∈ ℕ |
| 목표수익률 | R | 별%와 3/4 지정가 매도 기준에 공통 사용되는 목표수익률. | R (%) |
| 현재 평단가 | A | 현재 시점에 남아 있는 보유 포지션의 평균매입단가. | A = 총 보유원가 / H (H > 0) |
| 현재 보유수량 | H | 현재 시점에 남아 있는 보유 주식 수량. | H ≥ 0 |
| 잔금 | C | 전일 확정 체결 반영 후 다음 주문 생성에 즉시 사용할 수 있는 실제 가용현금. 자동매매에서는 주문 가능 여부와 계좌 정합성을 검증하는 핵심 상태값. | C_new = C_old - Σfilled_buy_amount + Σfilled_sell_amount |
| 총 매수누적액 | B_buy | 사이클 시작 후 체결 완료된 모든 매수금액의 누적합. 부분체결 포함. | Σ filled_buy_amount |
| 총 매도누적액 | B_sell | 사이클 시작 후 체결 완료된 모든 매도금액의 누적합. 부분체결 포함. | Σ filled_sell_amount |
| 순매수누적액 | B_net | T 계산에 사용하는 실제 소진 시드. 총 매수누적액에서 총 매도누적액을 차감한 값. | B_net = B_buy - B_sell |
| 별% | Star(T) | 회차 T에서 적용되는 일일 LOC 기준 퍼센트. 동일 의미를 전 버전 공통으로 사용. | Star(T) = R × (1 - 2T/a) |
| 별지점 가격 | S | 평단가 A에 Star(T)를 적용한 기준 가격. | S = A × (1 + Star(T)/100) |
| 1회 매수시도액 | M | 해당 주문세트의 기준 예산. 기본값은 사이클 시작 시 M = P / a로 정한다. V2.2에서는 실현손익·부분매도 때문에 사이클 중간에 M을 바꾸지 않는다. 다만 운영정책상 진행 중 외부 시드 증액을 반영하는 경우에는 재산정할 수 있다. | 기본값: M = P / a |
| 회차값 | T | 사이클 진행 정도를 나타내는 값. 분자는 실제 소진 시드인 B_net을 사용한다. | T = round_half_up(B_net / M, 1) |
| 쿼터매도 수량 | Q_sell | 당일 1/4 LOC 매도 수량. 정수 수량 기준으로 계산한다. | Q_sell = floor(H / 4) 또는 시스템 수량 규칙 |
| 쿼터손절모드 매수시도액 | M_q | 쿼터손절모드 진입 후 새로 계산되는 10분할 기준 예산. | M_q = available_cash_for_q / 10 |
| 쿼터손절 목표수익률 | R_q | 쿼터손절모드 내 LOC 매수·매도 기준에 사용되는 목표수익률. | 기본값은 R와 동일, 별도 설정 가능 |

<!-- table_4 -->
| 핵심 해석 잠금• T 계산의 분자는 “현재 평단가 × 보유수량”이 아니라 순매수누적액 B_net이다.• B_net은 실제 소진 시드로 해석하며, 총 매수누적액에서 총 매도누적액을 차감한 값이다.• 별% 표기는 전 버전 공통으로 Star(T)를 사용하고, S는 별지점 가격에만 사용한다. |
| --- |

2.1 핵심 계산식과 상태값 갱신 순서

<!-- table_5 -->
| 번호 | 일반식 | 설명 |
| --- | --- | --- |
| ① 순매수누적액 | B_net = B_buy - B_sell | 매도 체결이 발생하면 B_sell이 증가하므로 B_net과 T가 함께 재평가된다. |
| ② 별% | Star(T) = R × (1 - 2T/a) | 20분할/40분할 예시를 하나의 일반식으로 통일한다. |
| ③ 별지점 가격 | S = A × (1 + Star(T)/100) | 매수점은 정규화 후 0.01 차감, 매도점은 정규화 가격 그대로 사용한다. |
| ④ 잔금 | C_new = C_old - Σfilled_buy_amount + Σfilled_sell_amount | 전일 확정 체결 반영 후 다음 주문 생성에 사용할 수 있는 실제 가용현금이다. 주문 가능 여부와 계좌 정합성 검증에 사용한다. |
| ⑤ 수량 정수화 | qty = floor(amount / price) | 매수는 정수 내림이 기본이며, 잔돈 1주 보정 규칙은 별도 적용한다. |
| ⑥ 반올림 규칙 | round_half_up(x,1) | T는 언어 기본 round가 아니라 half-up 고정으로 소수 첫째 자리까지 반올림한다. |
| ⑦ 1회 매수시도액 | M = P / a (기본값) | 사이클 시작 시 총 원금과 총 분할 수로 정한다. 실현손익·부분매도 때문에 사이클 중간에는 M을 변경하지 않는다. 다만 V2.2는 구조상 운영정책에 따라 진행 중 외부 시드 증액을 반영해 M을 재산정할 수 있다. |

<!-- table_6 -->
| 상태값 갱신 우선순위• 전일 체결 확정 → B_buy, B_sell, H, A 갱신• → M 계산 또는 유지• → T 계산• → Star(T) 및 S 계산• → 모드 판정• → 해당 모드의 주문세트 생성 |
| --- |

3. 상태 판정과 주문 생성 파이프라인

<!-- table_7 -->
| 상태 | 판정 규칙 | 활성 주문세트 |
| --- | --- | --- |
| 첫 진입 | H = 0이고 새 사이클 시작일 | 상단 체결 보장 LOC + 동반 줍줍매수 사다리 |
| 전반전 | 0 < T < a/2 | 전반전 기본 주문세트 + 동반 줍줍매수 사다리 (필요 시 상단 매수부를 큰수매수로 재구성) |
| 후반전 | a/2 ≤ T ≤ a-1 | 후반전 기본 주문세트 + 동반 줍줍매수 사다리 (필요 시 상단 매수부를 큰수매수로 재구성) |
| 쿼터손절 진입일 | a-1 < T ≤ a 이고 직전 거래일까지 일반모드 | 진입일 전용 주문세트 |
| 쿼터손절 진행일 | 쿼터손절모드 활성 상태 | 쿼터손절 전용 주문세트 |

<!-- table_8 -->
| 순서 | 처리 단계 | 핵심 규칙 |
| --- | --- | --- |
| 1 | 전일 주문 정리 | 전일 미체결·부분체결·만료를 확정하고 잔량을 정리한다. |
| 2 | 체결 반영 | B_buy, B_sell, C, H, A 및 필요 자금 상태를 갱신한다. |
| 3 | 핵심 값 계산 | M, T, Star(T), S를 계산한다. |
| 4 | 상태 판정 | H, T, a 구간과 특수모드 진입 여부를 판정한다. |
| 5 | 기본 주문세트 생성 | 상태에 따라 첫 진입/전반전/후반전/특수모드 기본 주문세트를 계산한다. |
| 6 | 줍줍매수 동반 생성 | 기본 주문세트와 함께 하단 줍줍매수 사다리를 생성하고, 필요한 범위까지 단수를 확장한다. |
| 7 | 큰수매수 재구성 판정 | 브로커 가격 괴리 규칙으로 상단 매수부 거부 또는 거부 예상 시, 상단 매수부만 큰수매수 규칙으로 재구성한다. |
| 8 | 최종 주문세트 확정 | 기본 주문세트, 동반 줍줍매수, 필요 시 큰수매수 재구성 결과를 합쳐 최종 주문세트를 발행한다. |

4. 첫 진입과 일반모드 체결 규칙

4.1 첫 진입 (T = 0)

• 실행 조건: 이전 사이클이 종료되어 H = 0이고, 다음 거래일 새 사이클을 시작해야 할 때만 발동한다.

• 기본 가격: 전일 종가 대비 상단 버퍼(+15% 기본)를 적용한 체결 우선 LOC 가격을 사용한다.

• 기본 수량: 해당 주문세트 예산 전체를 가능한 한 많이 소진하는 정수 수량을 계산한다.

• 동반 주문: 첫 진입 기본 주문과 동시에 하단에는 줍줍매수 사다리를 함께 배치한다. 줍줍매수는 특수 옵션이 아니라 기본 주문세트의 동반 사다리다.

• 매도 주문: 첫 진입 시점에는 보유수량이 없으므로 매도 주문은 생성하지 않는다.

4.2 전반전 기본 주문세트

<!-- table_9 -->
| 항목 | 규칙 |
| --- | --- |
| 매수 예산 분배 | 1회 매수시도액을 금액 기준 50:50으로 분할해 상단/하단 기본 매수부를 만든다. 이후 기본 주문세트와 함께 줍줍매수 사다리를 동반 생성한다. |
| 상단/하단 배치 | 한 절반은 평단가 A, 다른 절반은 별지점 매수점 S_buy = normalize(S) - 0.01 에 LOC 매수한다. 두 주문이 기본 매수부이며, 하단에는 별도 줍줍 사다리가 추가된다. |
| 잔돈 1주 보정 | 두 기본 매수주문으로 남는 잔돈으로 추가 1주가 가능하면 A와 S 중 더 낮은 가격 주문부터 1주를 추가한다. 이는 기본 주문세트의 잔돈 보정이며, 줍줍매수와는 별개다. |
| 매도 공통 | Q_sell 만큼은 S에 LOC 매도, 나머지 H - Q_sell는 A × (1 + R/100)에 지정가 매도한다. 전반전 최종 주문세트는 “기본 주문세트 + 동반 줍줍매수 사다리”이며, 필요 시 상단 매수부만 큰수매수로 재구성한다. |

4.3 후반전 기본 주문세트

<!-- table_10 -->
| 항목 | 규칙 |
| --- | --- |
| 매수 구조 | 1회 매수시도액 전체를 S_buy에 LOC 매수한다. 평단가 LOC 매수는 제거한다. 이 기본 매수부와 별도로, 같은 예산 기준의 하단 줍줍매수 사다리를 항상 함께 생성한다. |
| 매도 구조 | 전반전과 동일하게 1/4 LOC 매도 + 3/4 지정가 매도를 유지한다. 후반전 최종 주문세트는 “기본 주문세트 + 동반 줍줍매수 사다리”이며, 필요 시 상단 매수부만 큰수매수로 재구성한다. |
| 의미 | 후반전은 평균단가를 위로 되돌리는 단계가 아니라 별지점 중심으로 체결 우선도를 높이는 단계로 해석한다. 줍줍은 계속 동반되고, 큰수매수는 상단 매수부 재구성에만 사용된다. |

5. 큰수매수 공통 알고리즘

큰수매수는 버전별로 다른 전략이 아니라, 일반모드 또는 특수모드 기본 주문세트가 브로커 가격 괴리 규칙에 의해 거부될 때 적용하는 공통 재구성 알고리즘이다.

<!-- table_11 -->
| 구분 | 규칙 |
| --- | --- |
| 발동 조건 | 현재가와 기본 LOC 주문가가 크게 벌어져 접수 거부 또는 거부 예상 상태가 확인될 때 |
| 기준점 설정 | 현재가보다 충분히 높은 버퍼 가격을 큰수 기준점 K로 정한다. K는 “해당 가격 이하에서 실제 매수가 진행되게 만드는 값”으로 선택한다. |
| 상단 물량 합산 | 기존 주문세트 중 K 이상 가격에서 체결될 예정이던 모든 매수 물량을 하나로 합산하여 K에 단일 LOC 매수한다. |
| 하단 사다리 유지 | 큰수 기준점 K 미만 구간의 하단 1주 주문과 기본 주문세트에 동반된 줍줍매수 사다리는 유지한다. 큰수매수는 상단 매수부만 압축하는 재구성 규칙이다. |
| 매도 처리 | 큰수매수는 매수 측 재구성 규칙이다. 매도 주문은 원칙적으로 기존 구조를 유지하되, 브로커가 매도까지 거부하면 매도 주문도 별도 안전정책에 따라 생략 또는 조정한다. |

<!-- table_12 -->
| 큰수매수 해석 고정• 큰수매수의 목적은 숫자 예시 재현이 아니라 상단 체결 보장이다.• 큰수 기준점 이상 가격에 있던 물량은 모두 합산하여 단일 LOC로 재구성한다.• 큰수매수는 매수측 주문구조를 바꾸는 로직이며, 매도 규칙을 새로 만드는 로직이 아니다. |
| --- |

6. 줍줍매수 공통 알고리즘

줍줍매수는 기본 주문세트와 함께 항상 생성하는 하단 예산 소진 사다리다. 주문을 내는 시점에는 당일 하락을 예측할 수 없으므로, 기본 주문세트와 동시에 하단 사다리를 미리 배치하여 해당 모드의 유효 예산을 가능한 한 넓은 가격 구간에 소진하도록 설계한다. 본 문서에서는 본질을 “기본 주문세트에 동반되는 하단 확장 규칙”으로 정의하고, 단수는 필요한 범위까지 확장 가능하다고 본다.

<!-- table_13 -->
| 항목 | 규칙 |
| --- | --- |
| 입력값 | 해당 모드의 유효 1회 매수시도액 M, 기본 주문세트로 계산된 기준 체결수량 Q0 |
| 가격식 | J_n = M / (Q0 + n), n = 1, 2, 3, ... |
| 발행 방식 | 각 n에 대해 1주 LOC 매수 주문을 생성한다. 가격은 센트 2자리 기준 올림 후 호가단위에 맞춰 정규화한다. 이 사다리는 기본 주문세트와 항상 함께 생성된다. |
| 중단 조건 | 예산이 허용하는 범위까지 단수를 확장한다. “6단”은 운영상 예시일 수 있으나, 문서 본질은 예산 소진을 위한 하단 사다리 확장 규칙이다. |
| 예시 프로파일 | 큰수매수로 상단 매수부를 합산한 경우에도 줍줍매수는 유지한다. 필요한 경우 합산 후의 Q0를 기준으로 단수를 다시 산정한다. |

7. V2.2 전용 특수모드: 쿼터손절모드

<!-- table_14 -->
| 항목 | 규칙 |
| --- | --- |
| 진입 조건 | a - 1 < T ≤ a 인 구간에 처음 진입한 거래일 |
| 진입일 매수 | 없음. 일반모드 매수와 병행하지 않는다. |
| 진입일 매도 | 보유수량의 1/4을 MOC로 강제 매도하고, 나머지 3/4는 A × (1 + R/100)에 지정가 매도한다. |
| 자금 재설정 | MOC 매도 대금 + 남은 잔돈을 합산하여 10분할하고 M_q를 새로 계산한다. |
| 진행일 매수 | 다음 거래일부터 M_q 전액을 A × (1 - R_q/100) 기준의 LOC 매수점(정규화 후 -0.01)에 배치한다. |
| 진행일 매도 | 보유수량의 1/4을 A × (1 - R_q/100) 가격에 LOC 매도하고, 나머지 3/4은 A × (1 + R/100)에 지정가 매도한다. |
| 탈출 규칙 | 쿼터손절모드 중 -R_q LOC 매도가 1회라도 체결되면 다음 거래일부터 일반 후반전으로 복귀한다. 복귀 후 1회 매수시도액은 해당 시점의 유효 M을 사용한다. 기본적으로는 사이클 시작값 M을 유지하되, 운영정책상 진행 중 외부 시드 증액을 반영한 경우에는 재산정된 M을 사용한다. |
| 연장 실패 | 10회 분할 자금이 다시 고갈되면 그날은 추가 매수 없이 1/4 MOC + 3/4 지정가 구조만 수행하여 현금을 확보한다. |

<!-- table_15 -->
| V2.2 특수모드 본질• V2.2의 쿼터손절모드는 일반 후반전의 연장이 아니라 별도 생존 매매 상태다.• 진입일에는 일반모드 주문과 혼합하지 않으며, 다음 거래일부터만 M_q 기반 주문세트를 발행한다.• 탈출 체결이 발생하기 전까지는 일반 후반전으로 복귀하지 않는다. |
| --- |

8. 체결 후 상태 갱신 규칙

<!-- table_16 -->
| 이벤트 | 갱신 규칙 |
| --- | --- |
| 매수 체결 | B_buy += filled_buy_amount; H += filled_qty; A를 가중평균으로 재계산; B_net 및 T 재계산 |
| LOC 1/4 매도 체결 | B_sell += filled_sell_amount; H -= filled_qty; 필요 시 실현손익 기록; B_net 및 T 재계산 |
| 3/4 지정가 매도 체결 | B_sell += filled_sell_amount; H -= filled_qty; H = 0이면 사이클 종료 후보로 표시 |
| 쿼터손절 진입 체결 | 진입일 MOC 매도 체결 후 M_q 계산; 다음 거래일부터 쿼터손절 진행 주문세트 생성 |
| 사이클 종료 | H = 0이면 해당 사이클 종료. 수익금을 새 총 원금으로 편입할지 여부는 운영정책에 따라 다음 사이클 시작 시점에만 반영 |

9. 기존 시나리오 범위의 규칙형 재배치

<!-- table_17 -->
| 기존 시나리오 번호 | 현재 문서에서 해석되는 규칙 위치 |
| --- | --- |
| ① 첫 진입 | 4.1 첫 진입 |
| ② 전반전 기본 | 4.2 전반전 기본 주문세트 |
| ③ 전반전 + 급락 | 5. 큰수매수 공통 알고리즘 |
| ④ 후반전 기본 | 4.3 후반전 기본 주문세트 |
| ⑤ 후반전 + 급락 | 5. 큰수매수 공통 알고리즘 |
| ⑥ 쿼터손절 진입일 | 7. V2.2 전용 특수모드 |
| ⑦ 쿼터손절 진행일 | 7. V2.2 전용 특수모드 |
| ⑧ 쿼터손절 진행일 + 급락 | 5. 큰수매수 + 7. V2.2 특수모드 |
| ⑨ 쿼터손절 탈출 다음날 | 7. 탈출 규칙 + 4.3 후반전 |
| ⑩ 쿼터손절 연장 실패 | 7. 연장 실패 규칙 |
| ⑪ 사이클 종료 다음날 | 4.1 첫 진입 |

10. 자동매매 구현 메모

<!-- table_18 -->
| 구현 항목 | 잠금 규칙 |
| --- | --- |
| 주문 생성 시점 | 전일 체결·만료·취소 확정 후 다음 거래일 주문 생성 직전에만 계산한다. |
| 미체결 처리 | 전일 미체결 잔량은 유지하지 않고 취소·만료 확인 후 다음 거래일에 재계산·재발행한다. |
| 부분체결 처리 | 부분체결 잔량은 고정 유지하지 않고 잔량 취소 후 전체 주문세트를 다시 계산한다. |
| 중복 방지 | 클라이언트 주문키 등 멱등성 키로 동일 주문세트의 중복 발행을 차단한다. |
| 하드가드 | 주문 생성 전 현금 부족·보유수량 부족·브로커 제한 위반 여부를 검사하고 초과 시 차단한다. |
| 비정상 이벤트 | 연속 거부, 호가 공백, 거래정지, API 장애 등 비정상 이벤트에서는 안전정지 또는 수동개입 상태로 전환한다. |
| 가격 정규화 | 모든 기준 가격은 호가단위에 맞게 먼저 정규화한다. 별지점 매수점만 정규화 후 0.01을 차감한다. |
| 쿼터손절 모드 배타성 | 쿼터손절 진입일과 일반모드 주문세트가 같은 날 동시에 발행되지 않도록 하나의 모드만 활성화한다. |
| 재분할 상한 | 쿼터손절모드의 M_q는 실제 가용현금 기준으로만 계산하며, 가상 현금이나 미래 체결 기대값을 포함하지 않는다. |

11. 문서 사용 시 주의사항

• 백테스트 해석: 본 문서는 실전 주문 생성 명세서다. 백테스트 엔진은 브로커의 LOC/MOC/지정가 체결 모델을 별도로 구현해야 한다.

• 수치 예시의 지위: 예시 숫자는 문서 중심이 아니다. 실제 구현은 일반식과 상태 규칙을 우선하며, 숫자 예시는 검산 보조 수단으로만 사용한다.

• 버전 차이 유지: 형식은 V4.0형으로 통일했지만, V2.2와 V3.0의 자금 철학과 특수모드 구조 차이는 그대로 유지한다.
